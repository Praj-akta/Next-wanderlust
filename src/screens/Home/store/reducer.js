// import Actions from './action';

const defaultState = {
}

export default function HomeReducer(state = defaultState, action) {
    switch (action.type) {

        default:
                return state;
        }
}